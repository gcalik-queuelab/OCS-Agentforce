<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Monthly_Contractual_Value_Growth__c</fullName>
    <description>Monthly Contractual Value Growth</description>
    <formula>IF( Contract_Term_Years__c &gt;= 1,
/* If Opp is for 12 or more months then divide by 12. */
Annual_Contractual_Value_Growth__c / 12,
/* If Opp is less than 12 months then divide by the number of months the Opp covers. */
/* If Opp covers 0 months then divide by 1 anyway to avoid error */
Annual_Contractual_Value_Growth__c / IF(Contract_Term_Months__c = 0, 1, Contract_Term_Months__c)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Monthly Contractual Value Growth</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Currency</type>
</CustomField>
