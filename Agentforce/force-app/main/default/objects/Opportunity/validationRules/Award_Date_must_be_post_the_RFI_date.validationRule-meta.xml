<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Award_Date_must_be_post_the_RFI_date</fullName>
    <active>true</active>
    <description>AD-304 RFP Submission Date. Only apply validation when at least one of the dates is changed.</description>
    <errorConditionFormula>AND(!$Setup.OCS_Config_Settings_CSH__c.Validation_Rules_Disabled__c,
				 RecordType.DeveloperName != &quot;Target&quot;,
				 NOT(TEXT(StageName) == &apos;7. Closed Won&apos;),
				 NOT( ISPICKVAL(Type,&quot;Rebid&quot; ) ),
     CloseDate&lt;RFI_Submitted_Date__c,
				OR( /* Only apply validation when at least one of the dates is changed to minimise flow errors */
								ISCHANGED(CloseDate),
								ISCHANGED(RFI_Submitted_Date__c)
								)
)</errorConditionFormula>
    <errorDisplayField>RFI_Submitted_Date__c</errorDisplayField>
    <errorMessage>Award Date must be post the RFI submission date</errorMessage>
</ValidationRule>
